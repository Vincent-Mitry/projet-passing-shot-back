<form method="post" action="{{ path('app_court_delete', {'id': court.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ court.id) }}">
    <button class="btn">Delete</button>
</form>
