{{ form_start(form) }}
    <div class="d-flex justify-content-center">
        <div class="col-9">
            {{ form_widget(form) }}
            <button class="btn btn-warning text-center">{{ button_label|default('Enregistrer') }}</button>
        </div>
    </div>
{{ form_end(form) }}
