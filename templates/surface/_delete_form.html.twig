
<form method="post" action="{{ path('app_surface_delete', {'id': surface.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette surface ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ surface.id) }}">
    <button class="btn">Delete</button>

</form>
