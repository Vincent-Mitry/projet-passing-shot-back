{% extends 'base.html.twig' %}

{% block title %}Création d'un utilisateur "back-office"{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class=" d-flex justify-content-center">
        <a href="{{ path('app_user_back-office') }}" class="btn btn-warning m-3">Retour</a>
    </div>
        <div class="card shadow">
            <div class="card-header py-3">
                <p class="text-danger m-0 fw-bold text-center">Création d'un utilisateur "back-office"</p>
            </div>
            <div class="card-body">
        <div class="table-responsive table mt-2" id="dataTable" role="grid" aria-describedby="dataTable_info">
            <table class="table table-striped table-hover my-0" id="dataTable ">
                <tbody>
                    <div class="m-3 p-5">{{ include('user/back-office/_form.html.twig') }}</div>
                </tbody>
            </table>
        </div>
{% endblock %}
