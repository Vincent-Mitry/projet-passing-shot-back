<form class=" d-flex justify-content-center" method="post" action="{{ path('app_user_back-office_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-warning m-3">Supprimer</button>
</form>
